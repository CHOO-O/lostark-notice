{
  "name": "lostark-notice",
  "version": "0.1.0",
  "private": true,
  "description": "Daily Lost Ark adventure island briefing sender for Discord webhooks.",
  "type": "module",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "sync:calendar": "node dist/scripts/syncCalendar.js",
    "notify:daily": "node dist/scripts/notifyDailyAdventureIsland.js",
    "start": "node dist/scripts/notifyDailyAdventureIsland.js",
    "check": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "typescript": "^5.5.4"
  }
}
